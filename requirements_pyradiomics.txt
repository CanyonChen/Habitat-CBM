# =============================================================================
# Pyradiomics 专用环境依赖
# 说明: pyradiomics 与 Python 3.12 不兼容，需要单独创建 Python 3.10/3.11 环境
# =============================================================================

# Python 版本要求: >=3.8, <3.12 (推荐 3.10 或 3.11)

# 核心依赖 (必须)
numpy>=1.24,<2.0          # 注意: 必须使用 1.x 版本，2.x 可能不兼容
SimpleITK>=2.3,<3.0       # 医学图像 I/O
pyradiomics>=3.0,<4.0     # 影像组学特征提取

# 构建依赖 (安装时需要)
versioneer>=0.29
pybind11>=2.10

# pyradiomics 自动安装的依赖（无需手动安装，列出供参考）
# PyWavelets>=1.1.1       # 小波变换
# pykwalify>=1.6.0        # YAML 验证
# ruamel.yaml>=0.16.0     # YAML 解析
# docopt>=0.6.2           # 命令行参数
# python-dateutil         # 日期处理
