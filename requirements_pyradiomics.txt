# =============================================================================
# Pyradiomics 专用环境依赖
# 说明: pyradiomics 与 Python 3.12 不兼容，需要单独创建 Python 3.10/3.11 环境
# =============================================================================

# Python 版本要求: >=3.8, <3.12 (推荐 3.10 或 3.11)

# 核心依赖 (必须)
numpy>=1.24,<2.0          # 注意: 必须使用 1.x 版本，2.x 可能不兼容
SimpleITK>=2.3,<3.0       # 医学图像 I/O
nibabel>=5.1,<6.0         # NIfTI 读写（build_habitat.py 需要）
pyradiomics>=3.0,<4.0     # 影像组学特征提取

# 数据科学与机器学习
pandas>=1.5               # 数据处理与分析
scikit-learn>=1.3         # LASSO/LR 模型、交叉验证、特征选择
joblib>=1.3               # 并行计算与模型保存

# 可视化
matplotlib>=3.7           # ROC 曲线、混淆矩阵等图表绘制

# 其他工具库
PyYAML>=6.0               # YAML 配置文件读写
tqdm>=4.65                # 进度条显示

# Habitat 掩膜构建的 GPU 加速（可选）
# 若希望 build_habitat.py 使用 --device cuda，请根据服务器 CUDA 版本
# 安装官方匹配的 PyTorch wheel；不要盲目在这里固定一个通用 torch 包。
# 例如参考: https://pytorch.org/get-started/locally/
# torch>=2.2,<3.0

# 构建依赖 (安装时需要)
versioneer>=0.29
pybind11>=2.10

# pyradiomics 自动安装的依赖（无需手动安装，列出供参考）
# PyWavelets>=1.1.1       # 小波变换
# pykwalify>=1.6.0        # YAML 验证
# ruamel.yaml>=0.16.0     # YAML 解析
# docopt>=0.6.2           # 命令行参数
# python-dateutil         # 日期处理
